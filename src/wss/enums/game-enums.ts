export const enum ShipTypes {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
  HUGE = 'huge',
}

export const enum CellStateTypes {
  EMPTY = 'empty',
  MISS = 'miss',
  SHOT = 'shot',
  SHIP = 'ship',
}

export const enum StatusTypes {
  MISS = 'miss',
  KILLED = 'killed',
  SHOT = 'shot',
}